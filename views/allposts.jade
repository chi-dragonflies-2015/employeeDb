extends layout

block content
  each post, i in posts
    h2= post.title
    h3= post.author
    p= post.body
    p= post.date
    //- form(method='POST', action='/posts/#{post._id}?_method=DELETE')
    //-   button(type='submit') Delete
